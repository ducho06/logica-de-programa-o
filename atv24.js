const prompt = require("prompt-sync")({sigint:true})

var preco=0, nvalor=0, desc=0

preco = Number(prompt("digite o preço: "))



desc= Number(prompt("digite o desconto: "))












if(!isNaN(preco)){
    if(!isNaN){
nvalor = preco-(preco *(desc/100))
console.log(nvalor)
    }
}